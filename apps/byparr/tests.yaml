---
# yaml-language-server: $schema=https://raw.githubusercontent.com/goss-org/goss/master/docs/schema.yaml
process:
  python:
    running: true
port:
  tcp:8191:
    listening: true
http:
  http://localhost:8191:
    status: 200
